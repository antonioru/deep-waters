import ofType from './ofType';

/**
 * Evaluates whether the given value is of type Uint8Array
 * @param value
 * @returns {boolean}
 */
const isUint8Array = ofType('Uint8Array');

export default isUint8Array;
