language: node_js
sudo: false
node_js:
  - "10"

install:
  - npm ci

script:
  - npm run lint
  - npm run test
  - npm run build

deploy:
  - provider: npm
    edge: true
    email: $NPM_EMAIL
    api_key: $NPM_TOKEN
    on:
      branch: master

